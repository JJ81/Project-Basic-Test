<h2 class="heading-s">비밀번호 설정</h2>

<div class="reset-pw">
    {{#if valid}}

        <form action="/reset/password/result" method="post">
            <div class="form-group">
                <input type="password" name="password" class="form-control txt_input" autocomplete="off" autofocus required placeholder="새로운 비밀번호를 입력하세요." />
            </div>
            <div class="form-group">
                <input type="password" name="re_password" class="form-control txt_input" autocomplete="off" required placeholder="새로운 비밀번호를 확인해주세요." />
            </div>

            {{#if error}}<div class="error">{{error}}</div>{{/if}}
            {{#if msg_password}}<div class="error">{{msg_password}}</div>{{/if}}


            <div class="btnArea clearfix">
                <button type="submit" class="btn btn-default btn-reset-pw">비밀번호 설정</button>
                <a href="/login" class="btn btn-default btn-login">로그인</a>
            </div>



            <input type="hidden" name="_csrf" value="{{csrfToken}}" />
            <input type="hidden" name="token" value="{{token}}" />
        </form>
    {{else}}
        <p>해당 링크의 만료 시일이 지났습니다.<br /> 비밀번호를 찾는 과정을 다시 진행해주세요.</p>
        <hr />
        <a href="/find/password" class="btn btn-primary">비밀번호 찾기</a>
    {{/if}}
</div>
