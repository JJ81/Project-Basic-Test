{{#if error}}
<div class="error">
    error : {{error}}
</div>
{{/if}}

<h2 class="heading-s">회원가입</h2>
<div id="signup">
    <form method="post" action="/signup" id="form_signup">
        <fieldset>
            <legend>회원가입</legend>
            <label for="username">*아이디</label>
            <input type="text" name="username" class="form-control txt_input" placeholder="아이디를 입력하세요." required autocomplete="off" autofocus value="{{usr_username}}" />
            {{#if username}}<p class="msg">{{username}}</p>{{/if}} {{! 콤마 대신 <br/> 태그가 들어갈 수 있도록 한다. }}

            <label for="nickname">*닉네임</label>
            <input type="text" name="nickname" class="form-control txt_input" placeholder="닉네임을 입력하세요." required autocomplete="off" value="{{usr_nickname}}" />
            {{#if nickname}}<p class="msg">{{nickname}}</p>{{/if}}

            <label for="password">*비밀번호 <small>(문자와 숫자를 포함하여 8자 이상 입력을 해야 합니다.)</small></label>
            <input type="password" name="password" class="form-control txt_input" placeholder="비밀번호를 입력하세요." required autocomplete="off" />

            {{#if password}}<p class="msg">{{password}}</p>{{/if}} {{!-- todo 메시지가 콤마를 기준으로 여러개가 나올 경우 개행이 되어서 출력이 될 수 있도록 한다  --}}

            <label for="re_password">*비밀번호(확인)</label>
            <input type="password" name="re_password" class="form-control txt_input" placeholder="비밀번호를 다시 입력하세요." required autocomplete="off" />
            {{#if re_password}}<p class="msg">{{re_password}}</p>{{/if}}

            <label for="email">*이메일  <small>(입력하신 이메일을 분실했을 경우, 아이디나 비밀번호를 찾을 수 없습니다.)</small></label>
            <input type="email" name="email" class="form-control txt_input" placeholder="이메일을 입력하세요." required autocomplete="off" value="{{usr_email}}" />
            {{#if email}}<p class="msg">{{email}}</p>{{/if}}

            <label for="market_code">마켓 코드 (선택)</label>
            <input type="text" name="market_code" class="form-control txt_input" placeholder="(선택) 마케팅코드를 입력해주세요." />

            <input type="hidden" name="_csrf" value="{{csrfToken}}" />

            <div class="agreement">
                <p class="txt">본 서비스의 원활한 이용을 위해 고객님의 개인정보수집과 이용약관에 동의하시겠습니까?</p>
                <span><input type="checkbox" required /> 이용약관</span>&nbsp;&nbsp;&nbsp;
                <span><input type="checkbox" required /> 개인정보취급방침</span>
            </div>

            <div class="btnArea clearfix">
                <button type="submit" class="btn btn-primary btn-register btn-block">가입하기</button>
                <a href="/" class="btn btn-default btn-home btn-block">홈으로</a>
            </div>

        </fieldset>
    </form>
</div>
